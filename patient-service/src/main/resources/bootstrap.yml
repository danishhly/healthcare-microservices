spring:
  application:
    name: patient-service
  config:
    # Remove 'optional:' to force the app to wait for the config server
    import: "configserver:http://localhost:8888"

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8762/eureka/
  instance:
    prefer-ip-address: true